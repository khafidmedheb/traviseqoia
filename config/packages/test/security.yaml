security:
    encoders:
        Symfony\Component\Security\Core\User\User: plaintext
    providers:
        in_memory:
            memory:
                users:
                    khafid:
                        password: 'sio22'
                        roles: 'ROLE_ADMIN'
    firewalls:
        secure_area:
            http_basic: ~
            # http_basic:
            #     realm: 'Secured Demo Area'
            #     provider: in_memory
        # secure_area:
            # provider: in_memory